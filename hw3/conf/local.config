params.conda_env_path = params.conda_env_path ?: System.getenv('CONDA_PREFIX')

conda.enabled = false

process {
    cpus   = 4
    memory = '8 GB'

    withLabel: 'assemble' {
        memory = '7 GB'
    }

    conda = params.conda_env_path
}
